"use client";
import{useState}from"react";import{ArrowRight,Sparkles}from"lucide-react";import{useRouter}from"next/navigation";
export default function Forgot(){const router=useRouter(),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);const data=Object.fromEntries(new FormData(e.currentTarget)),r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)}),j=await r.json();setBusy(false);setMessage(j.error||j.message);if(r.ok)setTimeout(()=>router.push("/signin"),900)}return <main className="auth"><section className="auth-art"><div className="brand"><Sparkles/> ROUTINE FLOW</div><div><p className="eyebrow">RESET YOUR ACCESS</p><h1>A fresh start<br/>is one step away.</h1></div></section><section className="auth-form"><div className="form-wrap"><p className="eyebrow">FORGOT PASSWORD</p><h2>Set a new password</h2><p className="muted">No email verification, just as requested.</p><form onSubmit={submit}><label>Email<input name="email" type="email" required/></label><label>New password<input name="newPassword" type="password" minLength="6" required/></label>{message&&<p className={message.includes("changed")?"notice":"error"}>{message}</p>}<button className="primary" disabled={busy}>{busy?"Updating…":"Change password"}<ArrowRight size={18}/></button></form><p className="switch"><a href="/signin">Back to sign in</a></p></div></section></main>}
